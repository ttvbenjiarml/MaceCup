# Arena Save/Regen and NBT Guide

Priority: FAWE schematic reset, WorldEdit schematic reset, Bukkit fallback. Bukkit fallback preserves ordinary block states and containers but cannot guarantee every NBT edge case. Use FAWE/WorldEdit for exact skulls, banners, frames, armor stands, shulkers, furnaces, PDC, and custom model data.
